## Frontend
# API base：浏览器请求前缀。
#   留空 = 同源 /api/chat（适用于部署在域根）
#   /chat = 子路径部署（如 https://host/chat/），让 fetch 走 /chat/api/chat
#   http://localhost:8012 = 直连后端（仅本地调试）
VITE_API_BASE=

# 部署子路径：Vite base，影响 index.html 里所有 asset 路径。
#   "/"      = 部署在域根
#   "/chat/" = 部署在子路径（与 VITE_API_BASE 对齐）
VITE_BASE_PATH=/

# 后端端口：决定 vite dev server 的 /api 代理到哪个后端
#   8011 = Coze 后端  (backend/app/main.py)
#   8012 = Dify 后端  (backend/app_dify/main.py)
VITE_BACKEND_PORT=8012
